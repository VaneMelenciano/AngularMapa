import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-mapa',
  webDir: 'dist/AngularMapa',
  bundledWebRuntime: false
};

export default config;
